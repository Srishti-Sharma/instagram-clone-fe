import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  root: {
    backgroundColor: "#FAFAFA",
    textAlign: "center",
    fontSize: 16,
    height: "100vh",
    // padding: 32,
  },
}));
